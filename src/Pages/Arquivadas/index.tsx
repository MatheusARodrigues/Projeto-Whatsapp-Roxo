import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import styles from './style';

const Arquivadas = () => {
    return (
        <View style={styles.container}>
            <Text style={styles.header}>Mensagens Arquivadas</Text>
        </View>
    );
};

export default Arquivadas;